@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "ایجاد حساب کاربری";
}

<h1>@ViewData["Title"]</h1>
<h4 id="external-login-title">خود را مرتبط کنید @Model.ProviderDisplayName اکانت.</h4>
<hr />

<p id="external-login-description" class="text-info">
    شما با موفقیت احراز هویت کردید <strong>@Model.ProviderDisplayName</strong>.
    لطفاً یک آدرس ایمیل برای این سایت در زیر وارد کنید و روی دکمه ثبت کلیک کنید تا کار تمام شود
    ورود به سیستم در.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">ایجاد حساب کاربری</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
