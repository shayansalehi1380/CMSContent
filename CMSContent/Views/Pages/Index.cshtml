@model IEnumerable<CMSContent.Models.Page>

@{
    ViewData["Title"] = " فهرست صفحات";
}

<h1><i class="fas fa-list-ul"></i>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus"></i> ساخت صفحه جدید</a>
</p>
<table class="table table-striped table-hover">
    <thead>
        <tr class="table-info">
            <th>
                @Html.DisplayNameFor(model => model.PageTitle)
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.PageShortText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PageText)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.PageDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PageIsPublished)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PageGroup)
            </th>
            <th>دستورات</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PageTitle)
            </td>
@*             <td>
                @Html.DisplayFor(modelItem => item.PageShortText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PageText)
            </td> *@
            <td>
                @Html.DisplayFor(modelItem => item.PageDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PageIsPublished)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PageGroup.PageGroupTitle)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.PageID" class="btn btn-info"><i class="fas fa-edit"></i> ویرایش</a>
                    <a asp-action="Details" asp-route-id="@item.PageID" class="btn btn-secondary"><i class="fas fa-info-circle"></i> جزئیات</a>
                    <a asp-action="Delete" asp-route-id="@item.PageID" class="btn btn-danger"><i class="fas fa-trash"></i> حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
